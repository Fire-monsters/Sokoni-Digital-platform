{
  "name": "@sokoni-digital/api-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:ci": "vitest run --passWithNoTests",
    "clean": "rimraf dist coverage"
  },
  "devDependencies": {
    "@sokoni-digital/domain": "workspace:*",
    "@sokoni-digital/typescript-config": "workspace:*",
    "@sokoni-digital/eslint-config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^3.0.0"
  }
}
